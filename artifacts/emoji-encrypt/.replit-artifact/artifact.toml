kind = "web"
previewPath = "/"
title = "Emoji Encrypt for LLM"
version = "1.0.0"
id = "artifacts/emoji-encrypt"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/emoji-encrypt run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/emoji-encrypt", "run", "build" ]
publicDir = "artifacts/emoji-encrypt/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
